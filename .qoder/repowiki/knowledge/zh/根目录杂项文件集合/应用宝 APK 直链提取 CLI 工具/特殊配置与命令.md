无需安装依赖即可运行；可通过 `npm link` 安装全局命令 `yyb-apk-extractor`。常用命令：`node index.js <包名|URL>` 提取直链，`node index.js search <关键词>` 搜索应用，`node index.js doctor` 检查本机 curl/aria2c/wget 环境，`node index.js --interactive` 进入交互式向导。测试使用 `npm test`（语法检查 + test/index.test.js），发布前可用 `npm run check` 执行 `npm pack --dry-run`。